import { PageContainer } from "@/components";

const UserProfileScreen = () => {
  return (
    <PageContainer title="Perfil de usuário">NÃO IMPLEMENTADO</PageContainer>
  );
};

export default UserProfileScreen;
